apiVersion: apps/v1
kind: Deployment
metadata:
  name: pingpong-dep
  labels:
    app: pingpong
spec:
  replicas: 1
  selector:
    matchLabels:
      app: pingpong
  template:
    metadata:
      labels:
        app: pingpong
    spec:
      containers:
        - name: pingpong
          image: aapolaivm/pingpong:v1.1
          command: [ "python", "/app/pingpong.py" ]  
          ports:
            - containerPort: 3001
          volumeMounts:
          - mountPath: /usr/src/app/files
            name: shared-volume
          resources:
            limits:
              memory: "128Mi"
              cpu: "500m"
      volumes:
      - name: shared-volume
        persistentVolumeClaim:
          claimName: shared-pvc


